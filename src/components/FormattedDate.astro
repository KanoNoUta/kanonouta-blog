---
interface Props {
	date: Date;
}

const { date } = Astro.props;
---

<time datetime={date.toISOString()} data-i18n-date>
	{
		date.toLocaleDateString('zh-CN', {
			year: 'numeric',
			month: '2-digit',
			day: 'numeric',
		})
	}
</time>
